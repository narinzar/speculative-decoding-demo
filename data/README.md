# data/

This demo generates from prompts passed on the command line, so it needs no
downloaded datasets. Model weights (distilgpt2, gpt2-large) are fetched by the
Hugging Face cache the first time you run a script, not stored here.

Put any custom prompt files you want to reuse in this folder. Everything here is
gitignored except this README.
