# outputs/

Benchmark results land here. `scripts/run_bench.py` writes `bench.json` with
wall time, tokens/sec, speedup vs the standard baseline, and mean acceptance
rate for standard, fixed-k speculative, and adaptive-k speculative decoding.

Everything here is gitignored except this README.
